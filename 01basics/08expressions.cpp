// Expressions

// Order of operators
  // ()    1
  // * / % 2
  // + -   3

  // x = a + b * c - d / e
    // b * c first
    // d / e second
    // a + b third
    // c - d fourth

  // x = (a + b) * (c - d) / e
    // (a + b) first
    // (c - d) second
    // () * () third
    // / e last

// Area of triangle
  // a = 1/2bh can't work!
  // a = (b * h) / 2 That's better
  // a must be a float

// Perimeter of Rectangle
  // p = 2(l + b)
  // p = 2 * (l + b)

// Sum of n terms
  // s=n(n+1)/2
  // s = n * (n + 1) / 2

// nth term of A*P Series
  // t = a + (n-1)d
  // t = a + (n - 1) * d

  // sqrt() is the square root function

// Quadratic
  // x = (-b + sqrt(b * b - 4 * a * c))/(2 * a)

// speed
  // s = (v * v - u * u) / (2 * a)
  // s = (pow(v, 2) - pow(u, 2))/ (2 * a)

