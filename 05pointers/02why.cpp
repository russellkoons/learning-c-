#include <iostream>

using namespace std;

int main()
{
  // The program can only access the stack and the code
  // Pointers can access the Heap, outside files, network connections, various hardwares
}